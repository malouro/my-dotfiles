#!/bin/sh

# NOTE: Newer versions of MacOS have an option to disable "Pointer acceleration" via the Settings UI.
# This script is probably moot now.

# Usage:
# no-mouse-accel

# Disables mouse acceleration on Mac - it's really annoying. :(
defaults write .GlobalPreferences com.apple.mouse.scaling -1
