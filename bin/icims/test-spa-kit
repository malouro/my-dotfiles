#!/bin/sh

REACT_SCRIPTS_DIR="$HOME/Projects/Work/icims-react-scripts"
TEST_DIR="$HOME/Projects/Test/fresh-test"

if [ -d "${TEST_DIR}" ]; then
	rm -rf ${TEST_DIR}
fi

yarn create react-app ${TEST_DIR} --scripts-version file:${REACT_SCRIPTS_DIR};
code ${TEST_DIR};
