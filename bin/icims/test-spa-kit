#!/bin/sh

REACT_SCRIPTS_DIR="$HOME/Projects/Work/icims-react-scripts"
TEST_DIR="$HOME/Projects/Test/fresh-test"

if [ -d "${TEST_DIR}" ]; then
	rm -rf ${TEST_DIR}
fi

npx create-react-app@3.2 --scripts-version file:${REACT_SCRIPTS_DIR} ${TEST_DIR};
code ${TEST_DIR};
